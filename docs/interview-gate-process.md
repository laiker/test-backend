# Сервис расчёта бонусов: Техническое задание

## 🎯 Назначение
Этот документ описывает требования к сервису расчёта вознаграждений. Сервис выполняет критически важную финансовую функцию, поэтому к нему предъявляются повышенные требования по надежности, производительности и качеству кода.

Тесты, приложенные к заданию, проверяют соответствие реализации этим требованиям.

---

## ⚙️ Бизнес-процесс

1.  **Инициализация:** Запускается команда расчёта за выбранный период.
2.  **Выборка:** Система получает список активных партнёров.
3.  **Расчёт:** Для каждого партнёра вычисляется бонус на основе истории его продаж.
4.  **Фиксация:** Результат сохраняется в базе данных.
5.  **Отчётность:** Выводятся диагностические метрики процесса.

---

## 📋 Требования к реализации (SLA)

### 1. Скорость доступа к данным
*   **Бизнес-требование:** Процесс получения истории продаж не должен становиться "узким местом". Время расчёта не должно кратно возрастать при увеличении количества партнёров.
*   **Критерий:** Общее время выполнения на тестовом наборе данных — **не более 0.30 sec**.

### 2. Эффективность использования ресурсов
*   **Бизнес-требование:** База партнёров постоянно растет. Система должна быть спроектирована так, чтобы обрабатывать списки любого размера (от сотен до миллионов) без существенного увеличения потребления оперативной памяти.
*   **Критерий:** Пиковое потребление памяти **до 6 MB** в тестовом сценарии.

### 3. Чистота входных данных (Data Integrity)
*   **Бизнес-требование:** Мы не можем начислять выплаты на основе некорректных данных. Если категория партнёра (`tier`) указана неверно или отсутствует, система должна явно сигнализировать об этом.
*   **Критерий:** Любые невалидные значения должны приводить к ошибке.

### 4. Модульность и гибкость архитектуры
*   **Бизнес-требование:** Источники данных для аналитики могут меняться. Архитектура должна позволять подменять механизм получения данных без переписывания основной логики расчёта.
*   **Критерий:** Код должен демонстрировать слабую связность. Основной сервис не должен зависеть от конкретной реализации хранилища.

### 5. Строгость контрактов данных
*   **Бизнес-требование:** Результаты расчётов передаются в смежные финансовые подсистемы. Для исключения ошибок интеграции формат передаваемых данных должен быть строго гарантирован.
*   **Критерий:** Метод аналитики должен возвращать **конкретную сущность (объект)**, описывающую контракт.

---

## 🧪 Как читать результаты тестов

В выводе консоли вы увидите следующие статусы:

- `ℹ️ INFO` — проверяемый аспект системы.
- `✅ SUCCESS` — требование выполнено.
- `❌ ERROR` — нарушение бизнес-требования (содержит ссылку на соответствующий пункт этого документа).